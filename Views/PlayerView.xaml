<UserControl x:Class="ex1.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ex1.Views"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Background="Pink">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
        <Button Margin="5">
            <Image Source="\images\playbutton.png" Height="29" Width="51"/>
        </Button>
            
        </StackPanel>
        
        <Slider Value="{Binding VM_Timestep, Mode=TwoWay}" HorizontalAlignment="Center" Margin="0,41,0,0" VerticalAlignment="Top" Width="436" Maximum="{Binding VM_Length, Mode=OneWay}" Height="26" Grid.Row="1"/>
        <Label Content="{Binding VM_Minute, Mode=OneWay}" HorizontalAlignment="Left" Margin="182,6,0,0" VerticalAlignment="Top" Height="30" Width="66" Grid.Row="1"/>
        <Label Content="speed:" HorizontalAlignment="Left" Margin="233,15,0,0" VerticalAlignment="Top" Grid.Row="1"/>
        <Slider Value="{Binding VM_Speed, Mode=TwoWay}" HorizontalAlignment="Left" Margin="282,21,0,0" VerticalAlignment="Top" Width="168" IsSnapToTickEnabled="True" TickFrequency="0.25" Minimum="0.25" Maximum="5" Grid.Row="1"/>
        <Label Content="{Binding VM_Speed, Mode=OneWay}" HorizontalAlignment="Left" Margin="179,132,0,0" VerticalAlignment="Top" Width="69" Height="27" Grid.RowSpan="2"/>
    </Grid>
</UserControl>
