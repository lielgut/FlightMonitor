<Window x:Class="ex1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ex1"  
        xmlns:views="clr-namespace:ex1.Views"
        mc:Ignorable="d"
        Title="FlightStat" Height="691" Width="982"
        ResizeMode="NoResize" Closed="Window_Closed" Visibility="Visible">
    <Grid Margin="10,10,10,11" Background="Gray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Margin="0,0,481,317" Grid.ColumnSpan="3">
            <views:PlayerView x:Name="playerView" Margin="0,0,0,10"/>
        </Grid>
        <Grid Margin="4,0,0,322" Grid.Column="2">
            <views:ResearchView x:Name="reserachView" Margin="-164,0,0,0"/>
        </Grid>
        <Grid Margin="0,337,481,0" Grid.ColumnSpan="3">
            <views:DataView x:Name="dataView" Margin="0,-10,0,0"/>
        </Grid>
        <Grid Margin="4,332,0,0" Grid.Column="2">

        </Grid>

    </Grid>


</Window>
